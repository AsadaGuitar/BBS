#token
expiration-time = 30 minutes
secret = "SECRET"

#db
logger.scala.slick=INFO
slick {
    profile = "slick.jdbc.PostgresProfile$"
    db {
      user = "postgres"
      password = "postgres"
      url = "jdbc:postgresql://localhost:5432/bbs_test"
      driver="org.postgresql.Driver"
    }
}